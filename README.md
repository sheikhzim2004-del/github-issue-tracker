# 1.What is the difference between var, let, and const?
ans: The 3 key is JS variable declare keyword. var let const there diffrents between to var is a function scoop. this is a old methot. let and const is a bloock scoop both but const variable dose not change after declar.

# 2.What is the spread operator (...)?
ans: This is a JS operator used to expand element by arry and object.

# 3.What is the difference between map(), filter(), and forEach()?
ans: map(), filter(), and forEach() is a function methot. The diffrence betweet to ---> Map takes all every elements and some works and return a new array with transforms values. filter takes a arry and make sure a new array with some specific elements. forEach creat a new array and with only the elements and run a function but dose not return anything.
 
 # 4. What is an arrow function?
 ans: Array function is a short and modern way to write function. The function is introduced in ES6 version.

 # 5. What are template literals?
 ans: Template literals (``) is a javascript feature insted of quata('', ""). That this is used to be write a string esely and write many dynamic elements insert variable in inside string.